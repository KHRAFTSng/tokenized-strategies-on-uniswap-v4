# Context Sources

Primary local references:
- `context/uniswap-docs/` (Uniswap docs repo)
- `context/unchain/` (Unchain docs repo)

Optional tracks:
- `context/uniswap/`
- `context/atrium/`
- `context/lending/`

These directories are used as source-of-truth references during implementation.
